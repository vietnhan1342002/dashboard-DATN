
export const metadata = {
    title: 'Login Page',
    description: 'Login Page',
}

export default function Login({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>
            {children}
        </>
    )
}
